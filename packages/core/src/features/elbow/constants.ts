/** Minimum distance from bound element edge to the first routing point. */
export const BASE_PADDING = 40;

/** Minimum segment length; segments shorter than this are removed. */
export const DEDUP_THRESHOLD = 1;

/** Corner radius for quadratic Bezier curves at elbow corners. */
export const ELBOW_CORNER_RADIUS = 16;
